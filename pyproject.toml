[project]
name = "mathematica-mcp-full"
version = "1.1.2"
description = "Full GUI control of Mathematica notebooks and kernel via Model Context Protocol"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Abhishek Singh Rawat"}
]
keywords = ["mathematica", "wolfram", "mcp", "llm", "ai", "notebook", "model-context-protocol"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    # <2 is required, not cautionary: mcp 2.x renamed FastMCP to MCPServer and
    # moved the module, so `from mcp.server.fastmcp import FastMCP` in server.py
    # raises ModuleNotFoundError on import and the server cannot start at all.
    "mcp>=1.0.0,<2",
    "wolframclient>=1.4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.4.0",
    "pydantic>=2.0.0",
]

[project.urls]
Homepage = "https://github.com/AbhiRawat4841/mathematica-mcp"
Repository = "https://github.com/AbhiRawat4841/mathematica-mcp"
Issues = "https://github.com/AbhiRawat4841/mathematica-mcp/issues"

[project.scripts]
mathematica-mcp-full = "mathematica_mcp.cli:main_cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mathematica_mcp"]

[tool.hatch.build.targets.wheel.force-include]
"addon" = "mathematica_mcp/addon"

[tool.hatch.build.targets.sdist]
include = [
    "src/",
    "addon/",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = ["src/mathematica_mcp"]

[tool.coverage.report]
exclude_lines = ["pragma: no cover", "if __name__", "raise NotImplementedError"]

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["mathematica_mcp"]
